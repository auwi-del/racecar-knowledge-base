{
  "name": "plan-listener",
  "description": "在 davinci-mini 开发板上部署和运行 /plan 话题监听器。注入 plan_listener.py 脚本、运行验证、保存路径 CSV 到 path_records/",
  "command": "plan-listener",
  "parameters": [
    {
      "name": "action",
      "description": "操作类型: deploy (注入并启动), run (仅运行已有脚本), status (检查运行状态), stop (停止监听器), download (下载路径CSV到本地)",
      "required": false,
      "default": "deploy"
    }
  ]
}
