# Nav2 导航调参架构图

> 生成时间：2026-06-07
> 说明：完整展现 Davinci-Mini 小车从传感器→融合→规划→控制的参数全链路

---

## 一、导航全链路架构总览

```mermaid
graph TB
    subgraph "📡 传感器层"
        LiDAR["激光雷达 LS LIDAR M10<br/>10Hz /scan"]
        Encoder["磁编码器<br/>50Hz /encoder"]
        IMU["九轴 IMU HIPnuc<br/>100Hz /IMU_data"]
        Camera["USB 摄像头<br/>30Hz /image_raw"]
    end

    subgraph "🧮 数据融合层"
        EKF["robot_localization EKF<br/>ekf_filter_node"]
        Odom["/odom_combined<br/>10Hz 融合里程计"]
    end

    subgraph "📍 定位层"
        AMCL["AMCL 自适应蒙特卡洛定位<br/>输入：/scan + /odom_combined<br/>输出：map→odom TF"]
        MAP["map_server<br/>地图加载：ai_map.yaml"]
    end

    subgraph "🗺️ 全局路径规划层 (planner_server)"
        SMAC["SmacPlannerHybrid<br/>Hybrid A* 算法"]
        PATH["/plan<br/>全局路径输出"]
        SMOOTHER["Smoother<br/>路径平滑后处理"]
    end

    subgraph "🎮 局部路径跟踪层 (controller_server)"
        RPP["RegulatedPurePursuitController<br/>受调节纯追踪"]
        CMD_VEL["/car_cmd_vel<br/>Twist 速度指令"]
    end

    subgraph "⚙️ 执行层"
        DRIVER["racecar_driver_node<br/>Twist → PWM 转换"]
        CHASSIS["下位机 /dev/car<br/>38400 波特率<br/>电机 PWM + 舵机 PWM"]
    end

    subgraph "🗺️ 代价地图体系 (Costmap)"
        GLOBAL["全局代价地图<br/>frame: map<br/>分辨率 0.2m/px<br/>静态层+障碍层+膨胀层"]
        LOCAL["局部代价地图<br/>frame: odom_combined<br/>4m x 4m 滚动窗口<br/>分辨率 0.05m/px<br/>体素层+膨胀层"]
    end

    subgraph "📋 航点/任务层"
        WP_CYCLE["waypoint_cycle<br/>RViz 点选→循环执行"]
        GO_CSV["go.py + CSV<br/>预置航点→逐点导航"]
        TELEOP["racecar_teleop<br/>键盘遥控"]
        ZHUITONG["zhuitong / lidar_tracking<br/>锥桶循迹"]
    end

    %% 连接线
    传感器层 --> EKF
    EKF --> Odom
    Odom --> AMCL
    LiDAR --> AMCL
    MAP --> AMCL
    AMCL --> SMAC
    代价地图层 --> SMAC
    SMAC --> SMOOTHER
    SMOOTHER --> PATH
    PATH --> RPP
    Odom --> RPP
    代价地图层 --> RPP
    RPP --> CMD_VEL
    CMD_VEL --> DRIVER
    DRIVER --> CHASSIS
  
    航点系统 --> CMD_VEL
    航点系统 --> SMAC

    %% 样式
    classDef sensor fill:#e1f5fe,stroke:#0288d1
    classDef fusion fill:#f3e5f5,stroke:#7b1fa2
    classDef localization fill:#fff3e0,stroke:#f57c00
    classDef planner fill:#e8f5e9,stroke:#388e3c
    classDef controller fill:#ffebee,stroke:#c62828
    classDef executor fill:#fce4ec,stroke:#880e4f
    classDef costmap fill:#e0f2f1,stroke:#00695c
    classDef task fill:#fafafa,stroke:#546e7a

    class LiDAR,Encoder,IMU,Camera sensor
    class EKF,Odom fusion
    class AMCL,MAP localization
    class SMAC,PATH,SMOOTHER planner
    class RPP,CMD_VEL controller
    class DRIVER,CHASSIS executor
    class GLOBAL,LOCAL costmap
    class WP_CYCLE,GO_CSV,TELEOP,ZHUITONG task
```

---

## 二、Planner Server 全局规划参数详解

```mermaid
graph LR
    subgraph "planner_server 参数体系"
        P1["minimum_turning_radius<br/>最小转弯半径<br/>当前: 0.6m"]
        P2["motion_model_for_search<br/>运动模型<br/>当前: REEDS_SHEPP"]
        P3["max_planning_time<br/>最大规划时间<br/>当前: 7.0s"]
        P4["angle_quantization_bins<br/>航向角离散化<br/>当前: 64 bins"]
        P5["cost_penalty<br/>障碍物代价惩罚🔥<br/>当前: 未设置(默认低)"]
        P6["cost_travel_multiplier<br/>代价行驶乘数🔥<br/>当前: 未设置(默认低)"]
        P7["tolerance<br/>规划容差<br/>当前: 未设置"]
    end

    subgraph "路径平滑器 (Smoother)"
        S1["w_smooth<br/>平滑权重<br/>当前: 0.5"]
        S2["w_data<br/>原始路径保留权重<br/>当前: 0.1"]
        S3["max_iterations<br/>最大迭代次数<br/>当前: 2000"]
    end

    PLANNER --> SMOOTH

    classDef highlight fill:#ffebee,stroke:#c62828,stroke-width:2px
    classDef normal fill:#e8f5e9,stroke:#388e3c
    class P5,P6 highlight
    class P1,P2,P3,P4,P7,S1,S2,S3 normal
```

---

## 三、代价地图参数体系

```mermaid
graph TB
    subgraph "全局代价地图 (Global Costmap)"
        G_RES["resolution: 0.2m/px<br/>较粗→计算快"]
        G_STATIC["static_layer<br/>地图静态数据"]
        G_OBS["obstacle_layer<br/>激光雷达实时障碍<br/>obstacle_max_range: 2.5m ⚠️"]
        G_INF["inflation_layer🔥<br/>膨胀半径: 0.15m ❌ 太小<br/>cost_scaling_factor: 10.0"]
    end

    subgraph "局部代价地图 (Local Costmap)"
        L_RES["resolution: 0.05m/px<br/>高精度"]
        L_VOXEL["voxel_layer<br/>体素层 Z轴 16层<br/>障碍物最大高度 2.0m"]
        L_INF["inflation_layer🔥<br/>膨胀半径: 0.25m ❌ 太小<br/>cost_scaling_factor: 10.0"]
    end

    subgraph "代价地图实际效果对比"
        E1["膨胀半径 0.15m<br/>路径与墙距离 ~0.15m<br/>❌ 几乎贴着走"]
        E2["膨胀半径 0.40m<br/>路径与墙距离 ~0.40m<br/>✅ 安全余量充足"]
    end

    GLOBAL --> LOCAL
    LOCAL --> EFFECT

    classDef danger fill:#ffebee,stroke:#c62828,stroke-width:2px
    classDef safe fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
    classDef normal fill:#e3f2fd,stroke:#1565c0
  
    class G_OBS,G_INF,L_INF danger
    class E1 danger
    class E2 safe
    class G_RES,L_RES,G_STATIC,L_VOXEL normal
```

---

### 四、Controller Server 路径跟踪参数

```mermaid
graph TB
    subgraph "速度控制"
        V1["desired_linear_vel<br/>目标线速度: 0.33 m/s"]
        V2["controller_frequency<br/>控制频率: 20 Hz"]
        V3["use_regulated_linear_velocity_scaling<br/>弯道减速: true✅"]
        V4["regulated_linear_scaling_min_speed<br/>弯道最低速度: 0.25 m/s"]
        V5["min_approach_linear_velocity<br/>接近终点最低速: 0.5🚨<br/>❌ 比目标速度还高"]
    end

    subgraph "前视距离 (核心参数)"
        L1["lookahead_dist: 0.3m ⚠️<br/>❌ 小于转弯半径 0.6m<br/>→ 左右晃动"]
        L2["min_lookahead_dist: 0.3m"]
        L3["max_lookahead_dist: 0.6m"]
        L4["use_velocity_scaled_lookahead_dist<br/>速度缩放前视: true"]
    end

    subgraph "安全控制"
        S1["use_collision_detection<br/>碰撞检测: false🚨<br/>❌ 不会主动避撞"]
        S2["max_allowed_time_to_collision<br/>碰撞预警时间: 未设置"]
        S3["allow_reversing: false<br/>不允许倒车"]
    end

    subgraph "转向控制"
        T1["use_rotate_to_heading<br/>原地旋转对准: false"]
        T2["rotate_to_heading_min_angle: 0.785 rad<br/>约 45度"]
        T3["rotate_to_heading_angular_vel: 0.4 rad/s"]
    end

    classDef danger fill:#ffebee,stroke:#c62828,stroke-width:2px
    classDef warning fill:#fff3e0,stroke:#f57c00
    classDef normal fill:#e8f5e9,stroke:#2e7d32

    class V5,S1 danger
    class L1 warning
    class V1,V2,V3,V4,L2,L3,L4,S2,S3,T1,T2,T3 normal
```

---

## 五、AMCL 定位参数

```mermaid
graph LR
    subgraph "AMCL 自适应蒙特卡洛定位参数"
        A1["max_particles: 2000<br/>最大粒子数<br/>↑定位越准, 计算越慢"]
        A2["min_particles: 500<br/>最小粒子数<br/>稳定后保持的下限"]
        A3["update_min_a: 0.06 rad<br/>旋转更新阈值<br/>约 3.4度"]
        A4["update_min_d: 0.025 m<br/>平移更新阈值"]
        A5["sigma_hit: 0.2<br/>激光击中标准差<br/>↑对测量更宽容"]
        A6["laser_model_type: likelihood_field<br/>似然场模型"]
        A7["z_hit / z_rand / z_short<br/>0.7 / 0.059 / 0.24<br/>激光模型概率权重"]
        A8["pf_err: 0.05 / pf_z: 0.99<br/>粒子滤波器参数"]
        A9["set_initial_pose: true<br/>自动设初始位姿(0,0,0)"]
    end

    classDef normal fill:#fff3e0,stroke:#f57c00
    class A1,A2,A3,A4,A5,A6,A7,A8,A9 normal
```

---

## 六、当前系统与实际链路对比

```mermaid
graph TB
    subgraph "🔴 当前实际运行链路"
        C1["Nav2 Planner (SMAC)<br/>生成全局路径✅"]
        C2["controller_server<br/>被注释❌"]
        C3["go.py / teleop / zhuitong<br/>Python脚本直发 /car_cmd_vel"]
        C4["racecar_driver_node<br/>PWM → 底盘"]
      
        C1 -.->|路径未使用| C2
        C2 -.-> C3
        C3 --> C4
    end

    subgraph "🟢 预期完整链路"
        E1["Nav2 Planner (SMAC)<br/>生成全局路径"]
        E2["Nav2 Controller<br/>RPP 路径跟踪"]
        E3["car_controller_new<br/>L1 路径跟踪 (预留)"]
        E4["racecar_driver_node<br/>PWM → 底盘"]
      
        E1 --> E2
        E2 --> E3
        E3 --> E4
    end

    classDef active fill:#e8f5e9,stroke:#2e7d32
    classDef inactive fill:#ffebee,stroke:#c62828,stroke-dasharray: 5 5
    classDef bypass fill:#fff3e0,stroke:#f57c00

    class C1 active
    class C2 inactive
    class C3,C4 bypass
    class E1,E2,E3,E4 active
```

---

## 七、参数调整优先级热力图

```mermaid
graph TB
    subgraph "🔥🔥🔥 一级调整 (解决80%问题—立即见效)"
        P1_1["1. 膨胀半径: 全局0.15→0.4, 局部0.25→0.5<br/>直接让路径远离墙壁"]
        P1_2["2. cost_penalty + cost_travel_multiplier<br/>路径居中, 不贴墙走"]
        P1_3["3. min_approach_linear_velocity: 0.5→0.15<br/>修复接近终点减速异常"]
        P1_4["4. lookahead_dist: 0.3→0.7<br/>增大前视消除晃动"]
    end

    subgraph "🔥🔥 二级调整 (进一步优化)"
        P2_1["5. use_collision_detection: false→true<br/>开启碰撞检测防撞"]
        P2_2["6. obstacle_max_range: 2.5→4.0<br/>扩大障碍物检测范围"]
        P2_3["7. desired_linear_vel: 0.33→0.28<br/>降低速度换稳定性"]
        P2_4["8. w_smooth: 0.5→0.7, w_data: 0.1→0.05<br/>更平滑的路径"]
    end

    subgraph "🔥 三级调整 (高级调优)"
        P3_1["9. use_rotate_to_heading: false→true<br/>大角度弯先旋转对准"]
        P3_2["10. angle_quantization_bins: 64→96<br/>更精细的航向搜索"]
        P3_3["11. motion_model: REEDS_SHEPP→DUBINS<br/>仅前进, 禁止倒车"]
        P3_4["12. max_planning_time: 7→10<br/>更长时间找更优路径"]
    end

    classDef urgent fill:#ffebee,stroke:#c62828,stroke-width:2px
    classDef normal fill:#fff3e0,stroke:#f57c00
    classDef advanced fill:#e3f2fd,stroke:#1565c0

    class P1_1,P1_2,P1_3,P1_4 urgent
    class P2_1,P2_2,P2_3,P2_4 normal
    class P3_1,P3_2,P3_3,P3_4 advanced
```

---

## 八、参数速查表

| 层级 |    模块    |                  参数                  |     当前值     |      建议值      |         作用         |
| :--: | :--------: | :------------------------------------: | :-------------: | :---------------: | :-------------------: |
| 规划 |  Planner  |            `cost_penalty`            |  未设置(默认)  | **2.0~3.0** | ⭐ 路径偏离障碍物意愿 |
| 规划 |  Planner  |       `cost_travel_multiplier`       |  未设置(默认)  | **2.0~3.0** | ⭐ 高成本区域惩罚倍率 |
| 规划 |  Planner  |       `minimum_turning_radius`       |       0.6       |  0.6 (匹配车体)  |     转弯半径约束     |
| 规划 |  Planner  |      `motion_model_for_search`      |   REEDS_SHEPP   |    REEDS_SHEPP    |       运动模型       |
| 规划 |  Smoother  |       `w_smooth` / `w_data`       |    0.5 / 0.1    |    0.7 / 0.05    |  平滑度 vs 原始路径  |
| 地图 |  全局膨胀  |          `inflation_radius`          | **0.15** |   **0.4**   |      🚨 安全余量      |
| 地图 |  局部膨胀  |          `inflation_radius`          | **0.25** |   **0.5**   |      🚨 安全余量      |
| 地图 |   障碍层   |         `obstacle_max_range`         |  **2.5**  |   **4.0**   |       检测范围       |
| 控制 | Controller |           `lookahead_dist`           |  **0.3**  |   **0.7**   |      ⭐ 前视距离      |
| 控制 | Controller |         `desired_linear_vel`         |      0.33      |     0.28~0.33     |       目标速度       |
| 控制 | Controller |    `min_approach_linear_velocity`    |  **0.5**  |  **0.15**  |    🚨 接近终点减速    |
| 控制 | Controller |      `use_collision_detection`      | **false** |  **true**  |      🚨 碰撞检测      |
| 控制 | Controller | `regulated_linear_scaling_min_speed` |      0.25      |       0.15       |      弯道最低速      |
| 控制 | Controller |       `use_rotate_to_heading`       |      false      |       true       |      大弯先旋转      |
| 定位 |    AMCL    | `max_particles` / `min_particles` |    2000/500    |       按需       |       定位精度       |
| 任务 |     BT     |          `goal_reached_tol`          |       0.6       |       按需       |       到达容差       |

---

> **相关文档索引**：
>
> - [知识库/15-导航调参指南.md](../知识库/15-导航调参指南.md) — 晃动/撞墙/犹豫根因分析
> - [知识库/18-nav.yaml配置详解.md](../知识库/18-nav.yaml配置详解.md) — 全参数逐项详解
> - [知识库/09-SLAM与导航.md](../知识库/09-SLAM与导航.md) — SLAM 建图与导航基础
> - [知识库/20-src目录调研.md](../知识库/20-src目录调研.md) — 当前系统状态分析
> - [知识库/21-配置文件有效性调研.md](../知识库/21-配置文件有效性调研.md) — 配置文件引用溯源
